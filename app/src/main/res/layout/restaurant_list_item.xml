<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="restaurant"
            type="com.android.wedyne.models.Restaurant" />

        <variable
            name="restaurantClickListener"
            type="com.android.wedyne.adapters.RestaurantAdapter.RestaurantClickListener" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/restaurantContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{() -> restaurantClickListener.onClick()}"
        android:layout_marginEnd="8dp"
        android:layout_marginBottom="8dp">

        <com.makeramen.roundedimageview.RoundedImageView
            android:id="@+id/restaurantImage"
            android:layout_width="0dp"
            android:layout_height="160dp"
            android:scaleType="centerCrop"
            android:src="@drawable/restaurant_image"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:riv_corner_radius="6dp" />

        <TextView
            android:id="@+id/restaurantName"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:fontFamily="sans-serif-medium"
            android:gravity="bottom"
            android:paddingStart="8dp"
            android:paddingBottom="8dp"
            android:text="@{restaurant.restaurantName}"
            android:textColor="@android:color/white"
            android:textSize="16sp"
            android:background="@drawable/restaurant_item_background"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@+id/restaurantImage"
            tools:text="AGASHIYE, AHMEDABAD" />

        <TextView
            android:id="@+id/restaurantRating"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:drawablePadding="4dp"
            android:layout_marginEnd="8dp"
            android:gravity="center_vertical"
            android:drawableStart="@drawable/ic_star"
            android:textColor="@android:color/white"
            android:textSize="14sp"
            android:text="@{restaurant.restaurantRating}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="3.0" />





    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>